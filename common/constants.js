'use strict';

// App enable
const APP_NOT_READY = 0;
const APP_READY = 1;

// Price modes
const PRICE_MODE_FLOW = 0;
const PRICE_MODE_INTERNAL = 1;
const PRICE_MODE_DISABLED = 2;

module.exports = {
  APP_NOT_READY,
  APP_READY,
  PRICE_MODE_DISABLED,
  PRICE_MODE_FLOW,
  PRICE_MODE_INTERNAL,
};
